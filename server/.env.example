# Game-server container secrets. Copy to .env (never commit .env).

# Shared HS256 secret — MUST be byte-for-byte identical to mc-auth's MC_JWT_SECRET.
# The auth plugin reads this from the environment to validate session cookies.
# Generate once: openssl rand -hex 48
MC_JWT_SECRET=
